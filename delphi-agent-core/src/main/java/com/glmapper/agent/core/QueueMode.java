package com.glmapper.agent.core;

public enum QueueMode {
    /**
     * 允许多个工具同时执行，适用于工具之间没有依赖关系或者可以并行执行的场景。
     * 这种模式下，代理可以同时调用多个工具，提高效率和响应速度。
     */
    ALL,
    /**
     * 一次只允许一个工具执行，适用于工具之间存在依赖关系或者需要串行执行的场景。
     * 这种模式下，代理在调用下一个工具之前必须等待当前工具执行完成，确保工具之间的顺序和正确性。
     */
    ONE_AT_A_TIME
}
